[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
# Start with correctness rules that can gate the current codebase. Expand this
# list as the existing style and import debt is cleaned up.
select = ["E9", "F63", "F7", "F82"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

